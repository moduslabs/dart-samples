name: HostBase
description: Common abstract base class for "hosts"
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.10.0 <3.0.0'

dependencies:
  mqtt_client: ^9.2.0
  mongo_dart: ^0.7.0
  debug:
    path: ../debug
  mqtt:
    path: ../mqtt
  JSON:
    path: ../JSON
  StatefulEmitter:
    path: ../StatefulEmitter
#  path: ^1.7.0

dev_dependencies:
  pedantic: ^1.11.0
