name: StatefulEmitter
description: Extended EventEmitter that fires "statechange" event when state member is changed.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.10.0 <3.0.0'

dependencies:
  eventify: ^1.0.0
  debug:
    path: ../debug
#  path: ^1.7.0

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
